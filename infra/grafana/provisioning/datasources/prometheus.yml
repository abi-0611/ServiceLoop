apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      # Matches the scrape interval. Grafana defaults to 15s regardless, and a
      # mismatch makes every rate() query look noisier than the data is.
      timeInterval: 15s
